{
  "name": "@pukeko-robot/web-client",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vue-tsc --build && vite build",
    "build-only": "vite build",
    "preview": "vite preview",
    "test": "vitest run",
    "type-check": "vue-tsc --build"
  },
  "dependencies": {
    "@galvanized-pukeko/vue-ui": "^0.0.1"
  }
}
